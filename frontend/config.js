// Deploy-time frontend config. Set API_BASE_URL to the backend (Zeabur) API origin, e.g.
//   window.APP_CONFIG = { API_BASE_URL: "https://atm-wigs-api.zeabur.app" };
// Leave it empty to let app.js fall back (localhost in dev, same-origin otherwise).
window.APP_CONFIG = { API_BASE_URL: "https://atmwigs-chatbot-sg.zeabur.app" };
